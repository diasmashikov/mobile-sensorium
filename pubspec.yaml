name: mobile_sensorium
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  audioplayers: ^5.2.1
  csv: ^5.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  path: ^1.8.3
  path_provider: ^2.1.1
  provider: ^6.1.1
  sensors_plus: ^4.0.1
  share: ^2.0.4
  sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/erzhan_wake_up.mp3
